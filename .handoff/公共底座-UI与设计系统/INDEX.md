# 公共底座 · UI 与设计系统交接索引

> 接手先读本文件，再按需展开单份文档。别一次全读。

本目录覆盖跨功能的 UI 层：设计稿对齐、图标体系、全局样式管理。
不属于任何单一功能，但六个功能的页面都长在这套底座上。

## 活跃

- [20260817 UI 设计稿对齐与样式集中](20260817-UI设计稿对齐与样式集中.md) — **最新** —
  全站 404 处 emoji → 92 个线性图标；缺图占位去卡通化；样式收敛到
  `miniapp/src/styles/` 三个文件；衣橱改左侧竖排分类栏。
  顺带修掉 4 个现存 bug（`TileImage` 的 `ratio` 从未生效、三个 tab 页双标题、
  free-match chips 竖排、closet 生成卡文字重叠）。
  五个坑写在文档里：**非组件模块放 `components/` 下会导致小程序白屏**、
  z-index 必须走变量、`.page` 要同时写 `height:100%` + `min-height:100vh`、
  scoped 规则不删全局样式不生效、CRLF 让脚本替换静默失效

## 已归档

- `archive/` —— 暂无

## 待办速览

| 项 | 状态 | 说明 |
|---|---|---|
| 图标体系（UiIcon + iconForEmoji） | ✅ 已完成 | 92 个图标；`vue-tsc` + 双端构建通过 |
| 缺图占位去卡通化 | ✅ 已完成 | 中性浅灰 + 线条图标，彩色渐变块已去除 |
| 样式集中（tokens/base/components） | ✅ 已完成 | 已删 60 条重复规则、44 处写死颜色；z-index 统一 |
| 个性化创建（图①）| ✅ 已对齐 | 工具栏去卡片、面板收窄；模特窄条是素材问题 |
| 我的衣橱（图③）| ✅ 已对齐 | 左侧竖排分类栏 + 两列网格 |
| 自由搭配（图④）| ✅ 已对齐 | 骨架本就一致，去卡片 + 修 chips 竖排 |
| 情景模拟（图⑤⑦）| ❌ 未做 | 需双 Tab + 天气条 + 推荐九宫格 + 底部三段 |
| 时尚杂志（图⑧）| ❌ 未做 | 现为 44 行跳转空壳 |
| 时尚社群（图⑨）| ❌ 未做 | 需搜索 + 城市 + 瀑布流 |
| 模特 / 衣橱素材 | ⚠️ 阻塞 | `front.png` 是 93×553 窄条，`outfit.png` 与 `closet/g1~g18.png` 全缺 |
| 页面 scoped CSS 清理 | ⚠️ 部分 | 8921 → 8454 行，剩余多为页面专属；用 `css-audit.mjs` 增量清 |
| 真机验证 | ❌ 未做 | 只在微信开发者工具里看过；重点验 base64 SVG 在安卓端 |
