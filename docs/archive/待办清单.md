# 待办清单（AI 穿搭助手）

> 配套文档：《项目满配蓝图》= `项目满配蓝图.md`（北极星，讲"为什么这么做"）。
> 这份是"具体做什么"，按阶段勾。起始日：2026-07-16。
> 铁律：① AI 逻辑手写、不用 LangChain（Express/FastAPI 照用）；② 前端+AI 往深挖，后端/数据/部署"会讲流程"即可。

---

## 🟢 阶段一 · 小程序能演示（蓝图步骤1 / 迁移 P2-P6）—— 先做这个

- [ ] 1.1 适配共享组件：PageHeader / SegTabs / SectionTitle / ProductCard / TileImage（`<img>`→`<image>`）
- [ ] 1.2 搬 5 个 tab 页：home / ai / closet / mall / me（closet 接后端 CRUD）
- [ ] 1.3 H5 截图验收一轮（5 个页能跑、布局不崩）
- [ ] 1.4 搬二级页：create / test / result / free-match / scene / magazine / community / stylist
- [ ] 1.5 特殊件：雷达图（canvas 手绘）、AvatarViewer 简化成静态图
- [ ] 1.6 联调：`dev:mp-weixin` 编译通过 + 写"怎么用微信开发者工具打开"文档

## 🟡 阶段二 · 后端规范化（蓝图步骤2）

- [ ] 2.1 拆三层：routes → services → repositories（对标 Spring Controller-Service-DAO）
- [ ] 2.2 SQLite → MySQL，建 7 张表：
      users / body_profiles / ★style_reports / garments / outfits / outfit_items / ★chat_sessions + ★chat_messages
      （每张业务表带 user_id 外键；高频字段加索引；图片只存 URL；AI 结果存 JSON 字段）
- [ ] 2.3 JWT 登录 + 越权控制（发 token，接口校验，非本人数据返 404）

## 🔴 阶段三 · AI 工程化核心 —— 上限所在，重点投入（蓝图步骤3）

- [ ] 3.1 结构化输出：风格报告用 JSON Schema 约束，存进 style_reports
- [ ] 3.2 流式输出：AI 顾问对话改 SSE 打字机效果，消息存 chat_messages
- [ ] 3.3 工具调用（手写）：tool-calling 循环——模型要工具→执行（查衣橱/天气）→喂回→循环到结束

## ⚪ 阶段四~七 · 进阶（有余力再上）

- [ ] 4.1 Python 门票：用 FastAPI 把 AI 层再写一版（intermediate 即可，岗位池翻倍）
- [ ] 5.1 MCP：把工具包成 MCP 暴露（对齐字节 AIDP 考点）
- [ ] 5.2 RAG + 向量库：穿搭知识切块→向量化→检索→拼 prompt
- [ ] 6.1 Element Plus 后台：衣服库管理 / 用户数据 / 图表（凑成 C+B 双端）
- [ ] 7.1 部署上线：后端上云 + HTTPS 域名 + 小程序配合法域名

---

## 优先级

阶段一、二 **必做** → 阶段三 **简历亮点、重点砸** → 四 **强烈建议** → 五 **差异化** → 六、七 **收尾**。
