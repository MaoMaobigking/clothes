# AI 服装（灵犀）· 开发手册

> 整合自 docs/ 原 5 份手册/规格文档（已归档到 `docs/archive/`）：AI-SETUP、DEPLOY、DESIGN_SPEC、MOCKUP-SPEC、IMAGE-MANIFEST。
> 战略/规划/路线图见 **《项目总纲》= `docs/项目总纲.md`**。
> 本手册是"怎么跑、怎么部署、怎么搭页面、图片怎么放"的操作参考。

## 目录

1. [开启真实 AI（3 步）](#1-开启真实-ai3-步)
2. [部署上线指南](#2-部署上线指南)
3. [页面搭建规范](#3-页面搭建规范)
4. [9 张设计稿 · 逐页结构规格](#4-9-张设计稿--逐页结构规格)
5. [图片清单（IMAGE-MANIFEST）](#5-图片清单image-manifest)

---

## 1. 开启真实 AI（3 步）

这个项目现在有**真实数据库**和**真实大模型**，不再全是写死的。

### 1.1 现在哪些是"真"的

| 功能 | 是真的吗 | 靠什么 | 需要 key 吗 |
|---|---|---|---|
| 衣橱（增/删/收藏/查） | ✅ 真数据库 | Node + SQLite（`server/data.db`） | 不需要 |
| 风格报告（结果页） | ✅ 真 AI | 大模型 | 需要 |
| 情景模拟 · AI 推荐 | ✅ 真 AI | 大模型（按天气+风格生成） | 需要 |
| AI 穿搭顾问（对话） | ✅ 真 AI | 大模型 | 需要 |
| 商品/杂志/社群等列表 | ❌ 还是 mock | 之后可入库 | — |

> 衣橱不需要 key：启动后端就是真数据库，能增删改查、刷新后还在。
> 其余三个 AI 功能需要填 key；没填会显示"AI 没连上"并回退本地示意，App 不崩。
> 密钥放在后端（`.env`），不会暴露到浏览器。

### 1.2 第 1 步：填你的 key

打开根目录的 **`.env`**，按你用的服务商改这几行（挑一行照着填）：

| 服务商 | AI_PROVIDER | AI_BASE_URL | AI_MODEL |
|---|---|---|---|
| DeepSeek | openai | https://api.deepseek.com | deepseek-chat |
| 通义千问 | openai | https://dashscope.aliyuncs.com/compatible-mode/v1 | qwen-plus |
| 智谱 GLM | openai | https://open.bigmodel.cn/api/paas/v4 | glm-4-flash |
| Kimi | openai | https://api.moonshot.cn/v1 | moonshot-v1-8k |
| OpenAI | openai | https://api.openai.com/v1 | gpt-4o-mini |
| Claude | anthropic | （留空） | claude-haiku-4-5-20251001 |

然后把 **`AI_API_KEY=`** 后面填上你的 key。例（DeepSeek）：

```
AI_PROVIDER=openai
AI_API_KEY=sk-你的key
AI_MODEL=deepseek-chat
AI_BASE_URL=https://api.deepseek.com
```

### 1.3 第 2 步：同时启动前端 + 后端

```bash
npm run dev:all
```

（也可分开：一个终端 `npm run server`，另一个 `npm run dev`。改了 `.env` 要重启 `server`。）

### 1.4 第 3 步：看效果

打开前端 → 首页「开始个性化创建 / 身形测试」→ 做完 5 步 → **风格报告页**。
- 顶部显示 **✨ 以下由 AI 实时生成**，下面总结、雷达分数、推荐配色、穿搭推荐、造型建议都是大模型**实时生成**。
- 点「换一份」重新生成。
- key 没填对会显示「AI 没连上」并自动回退本地示意数据（App 不崩）。

### 1.5 自检后端有没有配好

浏览器打开 <http://localhost:8787/api/health> ，看到 `"hasKey":true` 就说明 key 读到了。

### 1.6 这层怎么工作（架构）

```
浏览器(Vue)  ──/api/style-report──►  Node 后端(server/index.mjs)  ──►  大模型API
   结果页                              (保管 key、拼提示词、解析JSON)      (DeepSeek/通义/Claude…)
```

- 前端代码：`src/api/ai.ts`（发请求）、`src/views/ResultReport.vue`（展示）。
- 后端代码：`server/index.mjs`（注释很全，想加别的 AI 功能照着扩）。
- 想让**别的功能也接 AI**，在 `server/index.mjs` 里照着 `/api/style-report` 再加一个接口即可。

---

## 2. 部署上线指南

### 2.1 架构总览

```
用户 → 微信小程序 (uni-app) ──→ Nginx (HTTPS) ──→ Node.js (Express, port 8787)
                            ──→ FastAPI  (可选, port 8788)
                                                 ├── MySQL (3306)
                                                 └── SQLite (开发默认)
```

### 2.2 后端部署（阿里云轻量 / 腾讯云）

**① 环境准备**

```bash
# 安装 Node.js 18+
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 安装 MySQL
sudo apt-get install -y mysql-server
sudo mysql_secure_installation

# 或者用 Docker
docker run -d -p 3306:3306 \
  -e MYSQL_ROOT_PASSWORD=yourpassword \
  -e MYSQL_DATABASE=lingxi \
  --name lingxi-mysql mysql:8
```

**② 数据库初始化**

```bash
mysql -u root -p < server/schema.sql
```

**③ 配置环境变量**

```bash
# 生产 .env
cp .env .env.production
# 编辑 .env.production:
#   DB_TYPE=mysql
#   MYSQL_HOST=localhost
#   MYSQL_PASSWORD=yourpassword
#   JWT_SECRET=随机生成的长字符串
#   AI_API_KEY=你的真实key
```

**④ 启动后端**

```bash
# 安装 PM2
npm install -g pm2

# 启动 Node 后端
pm2 start server/index.mjs --name "lingxi-api" \
  --node-args="--experimental-sqlite" \
  --env-file .env.production

# 可选：启动 Python AI 层
cd ai-server && pm2 start "venv/bin/uvicorn main:app --host 0.0.0.0 --port 8788" --name "lingxi-ai"

pm2 save && pm2 startup
```

**⑤ Nginx 反向代理 + HTTPS**

```nginx
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name your-domain.com;

    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;

    location /api/ {
        proxy_pass http://127.0.0.1:8787;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # SSE 支持
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 300s;
    }

    location / {
        root /var/www/lingxi-h5;
        try_files $uri $uri/ /index.html;
    }
}
```

HTTPS 证书（Let's Encrypt 免费）：
```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

### 2.3 小程序上线

**① 编译生产包**

```bash
cd miniapp
npm run build:mp-weixin
```

**② 微信开发者工具操作**

1. 打开微信开发者工具 → 导入 `miniapp/dist/build/mp-weixin`
2. 填入 AppID（在 mp.weixin.qq.com 注册获取）
3. 在「开发管理 → 开发设置」配置：
   - 服务器域名：`https://your-domain.com`（request/upload/download）
   - 业务域名：`https://your-domain.com`
4. 点击「上传」提交代码
5. 微信公众平台 → 版本管理 → 提交审核
6. 审核通过后点击「发布」

**③ 注意事项**

- 个人主体小程序仅支持部分类目
- 所有请求域名必须是 HTTPS
- 图片/文件资源建议上传到云存储（OSS/COS）

### 2.4 H5 前端部署

```bash
cd miniapp
npm run build:h5
scp -r dist/build/h5/* user@server:/var/www/lingxi-h5/
```

### 2.5 管理后台部署

```bash
cd admin
npm run build
scp -r dist/* user@server:/var/www/lingxi-admin/
```

Nginx 配置中添加：
```nginx
server {
    listen 443 ssl;
    server_name admin.your-domain.com;
    root /var/www/lingxi-admin;
    # ... SSL 配置同上
    location / { try_files $uri $uri/ /index.html; }
}
```

### 2.6 快速检查清单

- [ ] 服务器防火墙开放 80/443 端口
- [ ] MySQL 已初始化 schema.sql
- [ ] .env.production 已配置真实密钥
- [ ] PM2 进程正常运行 (`pm2 status`)
- [ ] Nginx HTTPS 证书有效 (`certbot certificates`)
- [ ] 小程序后台已配置合法域名
- [ ] 真机扫码测试通过

### 2.7 监控 & 日志

```bash
# PM2 日志
pm2 logs lingxi-api

# Nginx 日志
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log
```

---

## 3. 页面搭建规范

> 适用于 **web（Vue3 + TS H5）** 端。你负责搭建**指定的一个页面**（一个 view 文件，复杂时可在自己的子文件夹里拆子组件）。严格照本规范，保证和已有页面风格一致。
> 迁移到 miniapp 时的适配（`<div>`→`<view>`、`@click`→`@tap`、`router`→`uni.*`）见《项目总纲》第 6.8 节。

### 3.0 铁律

1. **只创建你被分配的 view 文件**（及该页面专属子组件，放自己的子文件夹、用页面名前缀命名，如 `MallProductSheet.vue`）。
2. **绝对不要修改**这些共享文件：`router/index.ts`、`main.ts`、`App.vue`、`assets/styles/*`、`components/BottomNav.vue`、`components/PageHeader.vue`、`components/SegTabs.vue`、`components/TileImage.vue`、`components/ProductCard.vue`、`components/SectionTitle.vue`、`data/*`、`stores/*`。路由已接好，指向你要创建的文件路径。
3. 语言：Vue3 `<script setup lang="ts">`。TS 是 **strict + noUnusedLocals + noUnusedParameters**——**不要留没用到的 import / 变量 / 参数**，否则打包报错。
4. 不用任何真实图片。所有"图片"一律用 `<TileImage>` 占位（渐变色块 + emoji）。
5. 全中文文案，粉紫少女风，圆角卡片、柔和阴影、emoji 图标。参考 `views/Home.vue`、`views/Closet.vue`。

### 3.1 目录 & 别名

- `@` = `src`。例：`import TileImage from '@/components/TileImage.vue'`。

### 3.2 页面骨架（必须这样）

每个页面根是一个纵向 flex 的满高容器：

```vue
<template>
  <div class="page">
    <!-- 有底部 tab 的页面（首页/Ai/衣橱/商城/我的）：顶部可无 header -->
    <!-- 二级页面：顶部放 <PageHeader title="xxx" /> -->
    <PageHeader title="情景模拟" />

    <div class="body scroll-y hide-scrollbar">
      <!-- 你的内容 -->
    </div>

    <!-- 只有 5 个 tab 主页面才加这行；二级页面不要加 -->
    <BottomNav active="closet" />
  </div>
</template>

<style scoped>
.page { height: 100%; display: flex; flex-direction: column; }
.body { flex: 1; min-height: 0; padding: 12px 16px 16px;
        display: flex; flex-direction: column; gap: 14px; }
</style>
```

- 底部 tab 主页面（active 值）：`home` / `ai` / `closet` / `mall` / `me`。
- 二级页面用 `<PageHeader title="..." />`，自带返回键（默认 `router.back()`；可传 `to="/home"` 指定目标）。

### 3.3 设计变量（`assets/styles/variables.css` 已全局注入，直接用）

- 颜色：`--pink` `--pink-deep` `--purple` `--purple-deep` `--mint` `--mint-deep`
- 渐变：`--brand-gradient`（粉→紫，按钮/高亮用）、`--bg-gradient`（页面大背景，已在外壳设置）
- 文字：`--text-1`(主) `--text-2`(次) `--text-3`(弱) `--text-on-brand`(白)
- 面：`--surface`(白卡) `--surface-soft`(半透白) `--line`(描边)
- 圆角：`--radius-sm` `--radius` `--radius-lg` `--radius-pill`
- 阴影：`--shadow-card` `--shadow-float`
- 其它：`--gap`(16px) `--safe-bottom`

### 3.4 全局按钮类（`global.css` 已定义，直接加 class）

- `.btn`（基类）+ `.btn-primary`（粉紫渐变实心）/ `.btn-ghost`（白底）/ `.btn-text`（弱文字按钮）
- 滚动区加 `.scroll-y.hide-scrollbar`。

### 3.5 共享组件 API（直接 import 复用，别重造）

**`<TileImage>` —— 万能图片占位**
```
props: from?: string(默认粉) , to?: string(默认紫) , emoji?: string ,
       ratio?: string(默认 '1 / 1', 传 CSS aspect-ratio) , label?: string ,
       rounded?: string(默认 var(--radius))
```
例：`<TileImage from="#a6c8ff" to="#5f8bff" emoji="🧥" ratio="3 / 4" />`

**`<PageHeader>` —— 二级页顶部返回栏**
```
props: title: string , to?: string(返回目标, 不传则 router.back()) , sub?: string
slots: right(标题栏右侧)
```

**`<SegTabs>` —— 分段切换（v-model）**
```
props: tabs: {key:string,label:string}[] , modelValue: string
用法: <SegTabs v-model="tab" :tabs="[{key:'a',label:'虚拟试穿'},{key:'b',label:'AI推荐'}]" />
```

**`<SectionTitle>` —— 小节标题**
```
props: title: string , more?: string(右侧文字, 传了才显示)
emits: more (点右侧文字)
```

**`<ProductCard>` —— 商品/衣物卡**
```
props: title: string , price?: number , emoji?: string , from?: string , to?: string ,
       tag?: string , fav?: boolean , ratio?: string(默认 '1 / 1')
emits: click(卡片) , fav(点收藏心)
```

**`<BottomNav>` —— 底部 5 tab（只主页面用）**
```
props: active: 'home'|'ai'|'closet'|'mall'|'me'
它内部已用 router 跳转，你只需传 active。
```

### 3.6 Mock 数据（`@/data/mock.ts`，全部现成，直接 import）

- `CLOSET_CATEGORIES` / `GARMENTS`（衣橱 & 试穿用，含 emoji/from/to/price/category/brand/season）
- `MALL_CATEGORIES` / `MALL_PRODUCTS`（商城，含 price）
- `MAGAZINES`（时尚杂志封面）
- `POSTS` / `HOT_TOPICS`（时尚社群）
- `WEATHER`（城市/日期/温度/预警/未来几天 forecast）
- `OUTFIT_RECOS`（AI 推荐的整套搭配）
- `SCENES`（场景：游玩/职场/约会/运动…）
- `AI_TOOLS`（个性化创建右侧工具：更换性别/拍照换脸/造型优化/局部身材/收藏夹）
- `AI_FEATURES`（AI hub 入口：个性化创建/自由搭配/情景模拟/旧衣新生/新旧混搭）

类型都从 `@/data/mock.ts` 具名导出（`import { GARMENTS, type Garment } from '@/data/mock'`）。

### 3.7 Store（`@/stores/*`，需要交互再用）

- `useProfileStore`（个人画像，已存在）
- `useWardrobeStore`：`favIds`、`activeCategory`、`filtered`、`favoriteGarments`、`toggleFav(id)`、`isFav(id)`、`setCategory(key)`
- `useCartStore`：`count`、`ids`、`has(id)`、`add(id)`、`toggle(id)`

用法：`const cart = useCartStore()`，模板里 `cart.count`。

### 3.8 路由表（已接好）

`/home` `/ai` `/closet` `/mall` `/me` `/create` `/test` `/result` `/free-match` `/scene` `/magazine` `/community`

页面间跳转用 `const router = useRouter(); router.push('/scene')`。

### 3.9 交付

只输出：你创建了哪些文件（路径）+ 每个文件几句话说明 + 有没有需要主程接线的地方。不要贴整段代码。

---

## 4. 9 张设计稿 · 逐页结构规格

> 目标：每个页面的**布局、文字、分区、控件位置**都严格对齐设计稿。
> 图片一律用 `<TileImage :src="..." .../>`（src 取自 mock 的 img 字段），缺图自动回退占位。
> 所有页面沿用现有骨架/共享组件/CSS 变量（见第 3 章）。

### 图① Logo
青色圆角方块 + 白色描边「衣」字。→ App 图标，用于 Home 顶部品牌区 / 启动位。文件 `images/logo.png`。

### 图② 个性化创建页 → `views/CreateCenter.vue`
- 顶部栏：返回箭头 + 居中标题「个性化创建」+ 右上「完成」。
- 标题下一行：左上白色圆角标签「我的虚拟形象」。
- **中央**：正面全身模特图（`MODEL_IMAGES.front`，白背心+白短裤，站在**粉色椭圆台**上）。用 TileImage（ratio 约 3/4，透明底），下方加粉色椭圆"台子"阴影。模特右上飘小气泡「换发型」；膝盖附近「点击优化」小指示 + 圆点。
- **左侧浮层卡「基础信息」**：竖排 身高/体重/胸围/大腿围/小腿围/腰围，每行右侧✅圆勾；底部粉色「保存」按钮。
- **左侧中部**：竖排 5 个测试按钮 风格/肤色/脸型/体型/偏好测试（带小图标），点击 → `/test`。
- **右侧竖排工具栏**：基础信息/更换性别/拍照换脸/造型优化/局部身材/收藏夹（AI_TOOLS），图标在上、文字在下。
- **右下**：「自由搭配 →」入口 → `/free-match`。
- 该页是二级页，**保留 PageHeader，不放 BottomNav**（设计稿含底部导航，但本项目 CreateCenter 是二级页）。

### 图③ 我的衣橱 → `views/Closet.vue`
- 顶部：左上标题「我的衣橱」+ 右上筛选/排序小图标。
- **顶部品牌横条**：一行横向滚动品牌名 chips（BASIC/WARM/DENIM/ROMANCE/VINTAGE…，取衣物 brand 去重）。
- **左侧竖排分类**：全部/上衣/裤子/半身裙/连体装/鞋/包/帽子/首饰/配饰（CLOSET_CATEGORIES，带图标，当前项高亮）。
- **右侧网格**：2 列衣物卡，大图（TileImage :src=g.img, ratio 3/4）+ 底部小字（品牌 + 名称），右上角收藏心。
- 底部：BottomNav active="closet"。

### 图④ 自由搭配 → `views/FreeMatch.vue`
- 顶部：标题「我的虚拟形象」，右上一排小图标（收藏★ / 保存💾）。（或 PageHeader title="自由搭配"，右侧插槽放 ★/💾）
- **中央**：已穿搭全身模特图（`MODEL_IMAGES.outfit`，蓝外套+锈色灯芯绒裙+板鞋+帽）。
- **左侧竖排缩略**：一列可替换的小单品缩略图（取 wardrobe 几件），点了"换上"。
- **右侧竖排工具**：穿搭保存/更换模型/更换搭配/换鞋子/换裙子/进阶穿搭。
- **右下**：「个性化创建」入口 → `/create`。
- **底部面板**：两组 tab：「我的收藏」（全部/单衣/外套/裙装/裤装）、「试穿」（全部/T恤/衬衫/针织衫/风衣）。SegTabs 切大类 + 一排小分类 chips；下面横向/网格衣物缩略（:src=g.img）。
- 二级页，**不放 BottomNav**。

### 图⑤⑦ 情景模拟 → `views/SceneSim.vue`
- 顶部：返回 + 标题「情景模拟」，右上「穿搭日记 📔」。
- 顶部 SegTabs：**虚拟试穿 / AI推荐**。
- **天气卡**（两 tab 共用，SceneWeather）：📍重庆市渝北区；6月8日 星期日；左侧大号 23° + 天气图标 + 红色「暴雨预警」胶囊；右侧/下方未来三天：周一 30/22、周二 28/21、周三 29/20。
- **AI推荐 tab**：小标题「AI 搭配方式推荐」+ 右侧🔄换一套；右上「一键购买/一键搭配」；下面该套单品网格（OUTFIT_RECOS[i].pieces，:src=piece.img，3 列，图+名）。
- **虚拟试穿 tab**：「穿搭适用场景选择」+ 两个下拉/chips（游玩/职场…，SCENES）+「保存」；下面场景造型预览大图（:src=scene.img），带 ♥/★。
- **底部**：「穿搭搭配方式选择」三模式：新旧混搭/旧衣新生（中间高亮）/个性化定制（SCENE_MODES）。
- 二级页，**不放 BottomNav**，用返回。

### 图⑥ 商城（耳饰）→ `views/Mall.vue`
- 顶部：左上标题（当前分类名，如「耳饰」）+ 右上 logo/购物车图标。
- 分类横向 tab：耳饰/项链/戒指/手链/包袋（MALL_CATEGORIES）。
- **商品网格**：2 列，大图（:src=m.img, 1:1 白底）+ 名称 + **价格 ¥84.00 / ¥138.00**（ProductCard 已支持）。中间可插系列 banner（如「现货系列 / Bar clip series」）。
- **底部吸底**：「一键购买/一键搭配」两个粉色大胶囊。
- 底部：BottomNav active="mall"。

### 图⑧ 时尚杂志 → `views/Magazine.vue`
- 顶部：**左上圆形用户头像** + 中间/右侧 SegTabs「时尚杂志 / 穿搭思路」。
- 时尚杂志 tab：顶部一张**大号 BAZAAR 封面**（z1，:src，3/4，标题/副标题叠加）；下面 2 列网格其余封面（z2–z6，:src + tag + 标题）。
- 穿搭思路 tab：横向文章卡列表（左小封面 + 右 标题/tag/时长）。
- 二级页，**不放 BottomNav**。

### 图⑨ 时尚社群 → `views/Community.vue`
- 顶部：标题「时尚社群」+ 搜索框 + 右上「杭州 ▾」。
- **热门推荐 banner**：两条横条帖（p1/p2，左文右图，:src）。
- 热门话题 chips：HOT_TOPICS。
- **信息流瀑布流**（2 列）：用户实拍 OOTD（:src=p.img，高度错落）+ 正文两行 + 作者头像/名 + ❤️赞 + 💬评论。
- 二级页，**不放 BottomNav**。

### 通用改动（所有页）
1. 凡是"图片格子"，全部改成 `<TileImage :src="实体.img" ...>`（或 ProductCard 传 `:src`），保留 from/to/emoji 作为缺图回退。
2. 文案、分区标题、tab 名称严格按上面文字。
3. 保持骨架 `.page/.body`、滚动区、按钮类、CSS 变量不变。
4. 只有 5 个 tab 主页面（Home/AiHub/Closet/Mall/Profile）放 BottomNav；其余二级页用 PageHeader + 返回。

---

## 5. 图片清单（IMAGE-MANIFEST）

把图片放进 `public/images/` 对应位置，**文件名要和下表完全一致**，页面就会自动显示。
- 没放的图会自动回退到占位（渐变色块 + emoji），可**先放重点图，其余慢慢补**。
- 建议统一用 `.png`（支持透明，衣服/首饰抠图效果最好）；用 `.jpg` 就把下表文件名后缀改成 `.jpg`，但要同步告知。
- 尺寸只是建议，不严格；比例尽量接近，显示更好看。

### 5.1 Logo & 模特

| 文件路径 | 内容 | 建议比例/尺寸 |
|---|---|---|
| `images/logo.png` | App 图标（青色「衣」字） | 正方形 512×512，可透明 |
| `images/model/front.png` | 个性化创建页：白背心短裤**正面全身**模特（站在粉色圆台上） | 竖图 3:4，最好透明底 |
| `images/model/outfit.png` | 自由搭配页：已穿好整套的**全身造型**模特 | 竖图 3:4，最好透明底 |

### 5.2 衣橱衣物 `images/closet/`（对应「衣橱 / 自由搭配」）

| 文件 | 衣物 | | 文件 | 衣物 |
|---|---|---|---|---|
| `g1.png` | 宽松工装外套 | | `g10.png` | 机能束脚连体裤 |
| `g2.png` | 毛领派克大衣 | | `g11.png` | 厚底板鞋 |
| `g3.png` | 基础白T | | `g12.png` | 玛丽珍单鞋 |
| `g4.png` | 法式泡泡袖衬衫 | | `g13.png` | 通勤托特包 |
| `g5.png` | 直筒西装裤 | | `g14.png` | 红色链条包 |
| `g6.png` | 水洗牛仔裤 | | `g15.png` | 针织毛线帽 |
| `g7.png` | 灯芯绒半裙 | | `g16.png` | 银色耳夹 |
| `g8.png` | 百褶短裙 | | `g17.png` | 格纹羊毛围巾 |
| `g9.png` | 碎花连衣裙 | | `g18.png` | 羊羔毛夹克 |

建议：衣物平铺图或挂拍图，竖图 3:4，白/透明底最佳。

### 5.3 商城商品 `images/mall/`（对应「商城 / 耳饰」）

| 文件 | 商品 | | 文件 | 商品 |
|---|---|---|---|---|
| `m1.png` | C 形环扣耳夹 | | `m6.png` | 锁骨细链 |
| `m2.png` | 流苏长耳线 | | `m7.png` | 开口戒指 |
| `m3.png` | 海马造型耳饰 | | `m8.png` | 编织手链 |
| `m4.png` | 珍珠爱心耳钉 | | `m9.png` | 亚克力手镯 |
| `m5.png` | 几何银项链 | | `m10.png` | 迷你链条包 |

建议：商品图，正方形 1:1，白底。

### 5.4 时尚杂志封面 `images/magazine/`

| 文件 | 封面 |
|---|---|
| `z1.png` | BAZAAR（本期主打大封面，戴帽女士） |
| `z2.png` | VOGUE 春夏高定 |
| `z3.png` | ELLE 通勤穿搭 |
| `z4.png` | GQ 机能风 |
| `z5.png` | NYLON Y2K 复古 |
| `z6.png` | KINFOLK 莫兰迪配色 |

建议：杂志封面，竖图 3:4。

### 5.5 时尚社群用户穿搭 `images/community/`

| 文件 | 帖子 |
|---|---|
| `p1.png` | 小个子显高穿搭 |
| `p2.png` | 旧毛衣改马甲 |
| `p3.png` | 通勤 look 分享 |
| `p4.png` | 复古格纹皮革 |
| `p5.png` | 甜辣风穿搭 |
| `p6.png` | 莫兰迪配色 |

建议：用户实拍 OOTD 竖图（比例不限，越自然越好，社群做瀑布流）。

### 5.6 AI 推荐单品 `images/outfit/`（情景模拟「AI推荐」）

| 文件 | 单品 | | 文件 | 单品 |
|---|---|---|---|---|
| `o1-1.png` | 针织开衫 | | `o2-1.png` | 牛仔外套 |
| `o1-2.png` | 背心马甲 | | `o2-2.png` | 白色卫衣 |
| `o1-3.png` | 格纹衬衫 | | `o2-3.png` | 工装短裙 |
| `o1-4.png` | 红色手袋 | | `o2-4.png` | 帆布包 |
| `o1-5.png` | 毛线帽 | | `o2-5.png` | 厚底板鞋 |
| `o1-6.png` | 切尔西靴 | | `o3-1.png` | 泡泡袖衬衫 |
| `o3-2.png` | 碎花半裙 | | `o3-3.png` | 玛丽珍鞋 |
| `o3-4.png` | 珍珠耳饰 | | | |

建议：单品图，正方形 1:1，白底。（o1=雨天通勤、o2=周末游玩、o3=约会甜美）

### 5.7 情景模拟场景造型 `images/scene/`（虚拟试穿的场景预览）

| 文件 | 场景 |
|---|---|
| `play.png` | 游玩造型 |
| `work.png` | 职场造型 |
| `date.png` | 约会造型 |
| `sport.png` | 运动造型 |
| `party.png` | 派对造型 |

建议：模特在该场景下的全身造型，竖图 3:4。

### 5.8 优先级建议（想先看效果，先放这几张）

1. `model/front.png`、`model/outfit.png`（两个核心形象）
2. `mall/m1~m4.png`（耳饰页最显眼）
3. `magazine/z1.png`（杂志大封面）
4. `closet/g1、g2、g9、g18.png`（衣橱首屏）
5. `community/p1~p3.png`（社群首屏）

放完刷新页面即可看到效果，缺的自动占位。
