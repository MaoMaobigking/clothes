# 微信小程序体验版打包配置（uni build -p mp-weixin 时生效，H5 不受影响）
#
# 用法：复制成 .env.production，把域名填进去，然后 npm run mp:build:cloud
# 不填就跑 mp:build:cloud，scripts/pack-mp.mjs 会主动报错停下 ——
# 免得打出一个「能上传但真机图全裂」的包。

# ============ 方案 A：微信云托管（当前采用，见 deploy/云托管部署.md） ============
#
# 后端和 139 张演示图跑在同一个容器里，共用一个免备案的 HTTPS 域名。
# 域名要先加进 mp.weixin.qq.com → 开发设置 → 服务器域名的
# request / uploadFile 白名单，否则真机上所有接口都会被拦。

# 留空 = 关掉云开发那条链路，前端直连下面的域名。
VITE_CLOUD_ENV=

# 接口地址。miniapp/src/utils/request.ts 读它。
VITE_API_BASE_URL=https://api-你的环境ID.ap-shanghai.run.tcloudbase.com

# 静态图前缀。主包上限 2 MB 而 static/images 有 3.2 MB，图必须出包。
# 容器把它们挂在 /images（见 server/index.mjs 的 imagesDir）。
# 编译期替换源码里的字面量（vite.config.ts），运行期再补一刀接口返回的路径
# （utils/cloud.ts 的 rewriteAssetPaths）—— 两处都靠这一个变量。
VITE_CLOUD_IMG_BASE=https://api-你的环境ID.ap-shanghai.run.tcloudbase.com/images


# ============ 方案 B：云开发 + 云函数转发（已放弃，代码还留着） ============
#
# 适用于「后端跑在裸公网 IP 上」：小程序 wx.request 只认已备案域名，
# IP 填不进白名单，而云函数出网不受这个限制，所以拿云函数当转发层。
# 走这条就换成下面三行，并给 cloudfunctions/api 配好环境变量 API_ORIGIN。
#
# VITE_CLOUD_ENV=你的云开发环境ID
# VITE_API_BASE_URL=
# VITE_CLOUD_IMG_BASE=cloud://你的云开发环境ID.数字后缀/images
