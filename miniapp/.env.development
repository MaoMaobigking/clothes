# 开发环境配置（uni dev / uni dev -p mp-weixin 时生效）
#
# 这个文件**入库**：里面只有本机地址，没有任何密钥。
# 生产配置在 .env.production（不入库，照 .env.production.example 自己建）。
#
# 只有 VITE_ 开头的变量会被打进前端代码（import.meta.env 读）。

# ============ 接口地址 ============

# H5 留空 = 走同源 /api/*，由下面的 dev proxy 转发到后端。
# 这样开发时不用配 CORS，部署时后端自己托管 H5 产物，前后端天然同域。
# 只有要让 H5 直连某个远程后端时才填绝对地址。
VITE_API_BASE_URL=

# 小程序端专用：小程序没有「同源」，必须写全 URL。
# 127.0.0.1 只在微信开发者工具里有效 —— 真机预览时它指的是手机自己，
# 要用真机调试就改成你电脑的局域网 IP，例如：
#   VITE_MP_DEV_API_BASE_URL=http://192.168.1.5:8787
# 手机和电脑要在同一个 Wi-Fi，且开发者工具里勾上「不校验合法域名」。
VITE_MP_DEV_API_BASE_URL=http://127.0.0.1:8787

# ============ dev server 代理 ============

# vite dev server 把 /api 和 /uploads 转发到哪（见 vite.config.ts）。
# 接同事的后端、或后端换了端口，改这一行就行。
# 命令行的 API_PROXY_TARGET 优先级更高，可以临时覆盖而不改文件。
VITE_DEV_PROXY_TARGET=http://localhost:8787

# ============ 云开发 ============

# 开发期一律留空 = 关掉云开发链路，直连上面的地址。
# 只有打体验版才需要填，见 .env.production.example。
VITE_CLOUD_ENV=
VITE_CLOUD_IMG_BASE=
