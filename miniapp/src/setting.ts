/*
 * 全局品牌 / 应用配置 —— 改这里，全局生效。
 *
 * 对应参考项目的 src/setting.ts。加它的直接原因：品牌名原先散落 8 处，
 * 而且写法互不一致（灵犀 / 灵犀穿搭 / 灵犀 AI 穿搭 / 灵犀甄选 / 灵犀严选），
 * 改个名字要全库搜。
 *
 * ⚠️ 有两处接不进来，是 uni-app 的硬限制，不是遗漏：
 *   - src/pages.json 的 navigationBarTitleText
 *   - src/manifest.json 的 name
 * 它们是静态 JSON，编译期读取，不能 import TS。改品牌名时记得同步这两个文件
 * （目前都写着「AI 服装」/「AI服装」，与下面的 name 不一致，属历史遗留）。
 */
export default {
  /** 应用简称。对话、口播里用它 */
  name: '灵犀',

  /** 完整名。海报署名、分享文案用它 */
  fullName: '灵犀 AI 穿搭',

  /** 一句话定位，登录页副标题这类地方用 */
  slogan: '你的私人穿搭助手',

  /**
   * 商品默认品牌名。
   * 注意和 shopBrand 是两个东西：这个是「商品没填品牌时显示什么」，
   * 下面那个是商城频道自己的名字。原先分别写死成「灵犀甄选」「灵犀严选」，
   * 保持原样没有合并 —— 合不合并是产品决定，不是重构该顺手做的事。
   */
  goodsBrand: '灵犀甄选',

  /** 商城频道名 */
  shopBrand: '灵犀严选',

  /** 应用 logo */
  logo: '/static/images/logo.png',
}
